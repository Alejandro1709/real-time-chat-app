# Real-Time Chat Application using ReactJS, NodeJS, MongoDB and Socket.io

## Installation Steps:

1. Clone or fork this repo.
2. Run the `npm install` command at the root of the project.
   [Build in progress]
